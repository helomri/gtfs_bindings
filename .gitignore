# https://dart.dev/guides/libraries/private-files
# Created by `dart pub`
.dart_tool/

# Avoid committing pubspec.lock for library packages; see
# https://dart.dev/guides/libraries/private-files#pubspeclock.
pubspec.lock

# Do not publish a copy of the Dart-generated documentation as it could be left out with outdated information.
/doc
/transit-spec

# They got in because I use path_provider at some point in the example.
.flutter-plugins
.flutter-plugins-dependencies

# I am also adding editor files which in my case aren't a security risk but could be for anyone else.
# IntelliJ
*.iml
*.ipr
*.iws
.idea/

# Mac
.DS_Store

